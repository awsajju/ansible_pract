- name: install nginx
  hosts: web
  become: yes
  tasks: 
  - name: installing nginx
    ansible.builtin.package:
      name: nginx
      state: installed
  - name: started nginx
    ansible.builtin.service:
      name: nginx
      state: present